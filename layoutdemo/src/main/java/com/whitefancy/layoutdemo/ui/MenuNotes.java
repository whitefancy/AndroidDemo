package com.whitefancy.layoutdemo.ui;

public class MenuNotes {
    //Menu菜单文件
    //menu res在单独的menu文件夹
    // xml文件的根结点必须是menu，包含 item 或group 元素
    //Xmlns（XML命名空间）取值必须是 http://schemas.android.com/apk/res/android
    // item 菜单项，可能包含一个<menu>元素，如果有子菜单
    //Android：id 资源id，为资源定义的唯一id，带加号表示用户自定义的id，不带加号表示系统统一定义的id
    //android:title 菜单标题
    //<group>菜单组，用于创建共享特征的项目集合。包含多个item
    //菜单分为 options菜单（右上角）和浮动上下文菜单（完成对用户长按事件的响应）弹出菜单
    //
    
}
